<div class="sections_wrapper">
    {{#each sections}}
        <section class="section_container">
            <h1>{{this.title}}</h1>
            {{log this.title}}
            {{#each this.items}}
                <h3>{{this.snippet.title}}</h3>
                <img src="{{this.snippet.thumbnails.medium.url}}"/>
                <button class="addToPlaylist" type="button" name="addToPlaylist">Add +</button>
            {{/each}}
        </section>
    {{/each}}
</div>